export default function SettingsPage() {
  return (
    <section aria-labelledby="settings-title">
      <h1 id="settings-title" className="text-xl font-semibold">
        Settings
      </h1>
      <p className="mt-4 text-sm text-zinc-500">Empty. No preference fields in W0.</p>
    </section>
  );
}
